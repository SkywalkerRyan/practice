// LC 547
function findCircleNumTrav(isConnected: number[][]): number {
	let n = isConnected.length;
	let visited: number[] = new Array(n).fill(false);

	let cnt = { province: 0 };
}

function dfs(cur: number, visited: boolean[]): void {
	visited[cur] = true;

	for (let i = 0; i < array.length; i++) {
		
	}
}
